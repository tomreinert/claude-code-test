# Claude Code Test

A simple test website deployed via GitHub Pages.

## Deployment

This site is deployed automatically by pushing to the `main` branch or via pull request.

## Local Development

Open `index.html` in your browser to view the site locally.
